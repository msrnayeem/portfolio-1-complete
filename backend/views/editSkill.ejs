<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Skill</title>

    <!-- Link to Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
</head>

<body class="container mt-5">

    <h1>Edit Skill</h1>

    <form action="/ejs/skills/update/<%= skill._id %>" method="POST" onsubmit="return validateForm()">
        <div class="mb-3">
            <label for="skillName" class="form-label">Skill Name</label>
            <input type="text" class="form-control" id="skillName" name="skillName" value="<%= skill.skillName %>"
                required>
            <div class="invalid-feedback">Please enter a valid skill name.</div>
        </div>

        <div class="mb-3">
            <label for="skillDetails" class="form-label">Skill Details</label>
            <textarea class="form-control" id="skillDetails" name="skillDetails" rows="3"
                required><%= skill.skillDetails %></textarea>
            <div class="invalid-feedback">Please enter valid skill details.</div>
        </div>

        <button type="submit" class="btn btn-primary btn-sm">Update Skill</button>
        <a href="/ejs/getSkills" class="btn btn-sm btn-secondary">Cancel</a>
    </form>

    <!-- Link to Bootstrap JS (optional, if you need Bootstrap JavaScript features) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL"
        crossorigin="anonymous"></script>

    <script>
        function validateForm() {
            const skillName = document.getElementById('skillName').value;
            const skillDetails = document.getElementById('skillDetails').value;

            // Add your custom validation logic here
            if (skillName.trim() === '' || typeof skillName !== 'string' || skillDetails.trim() === '' || typeof skillDetails !== 'string') {
                alert('Please enter valid skill information.');
                return false;
            }

            return true;
        }
    </script>
</body>

</html>